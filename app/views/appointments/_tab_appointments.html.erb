<h5><%= I18n.t ".appointments.next_meetings", count: candidate.appointments.size %> :</h5>
  <%if candidate.appointments.empty? %>
    <p>Vous n'avez pas encore de rendez-vous prévu avec ce candidat.</p>
  <%else%>
    <% candidate.appointments.order(:begins_at).each do |appointment|%>
      <p><%= raw I18n.l(appointment.begins_at, format: :full_date_with_time)%>
      <%= link_to edit_appointment_path(appointment) do%>
        <i class="fa fa-pencil-square-o" aria-hidden="true"></i>
      <%end%>
      <%= link_to mission_candidate_appointment_path(mission, candidate, appointment), method: :delete, data: { confirm: "Etes-vous sure?" } do %>
        <i class="fa fa-trash" aria-hidden="true"></i>
      <%end%>
    <%end%>

    </p>

  <%end%>

  <!-- Ajouter une condition sur le statut du candidat -->

